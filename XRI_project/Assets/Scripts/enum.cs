public enum FeatureUsage
{
    Once, //use once
    Toggle //use more than once
}

public enum FeatureDirection
{
    Forward, 
    Backward

}

public enum GameState
{
    Playing,
    Paused,
    ChallengeSolved
}